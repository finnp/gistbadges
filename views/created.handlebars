<div class="jumbotron">
<h1>You created your badge!</h1>

<img src="{{imageurl}}" alt="Badge" />

<p>Now copy this link and send it to the earner
<input type="text" class="form-control" value="{{shareurl}}"/>
</p>
You can also view the <a href="{{gisturl}}" target="_blank">Gist</a> for the badge.

</div>